/// The format of the transcript output.
public enum TranscriptionFormat: String {
    case json = "json"
    case text = "text"
    case srt = "srt"
    case verboseJSON = "verbose_json"
    case vtt = "vtt"
}
